# MEMORY INDEX  ·  keep ≤ ~80 lines

## State            (rewrite in place — current truth only, ≤ ~10 lines)
- claudeBrain is a **factory** for Claude Code assets: `.claude/` authors meta-tooling; `example-project/` showcases a produced `.claude/`.
- **example-project** carries consumer agents: `finance-quantitative-developer` (Python quant, subjective executor) + an **objective/conceptual agent family** — `data-analyst` (explore/visualize/conclude), `software-architect` (project structure/placement/naming/conventions), and `presentation-architect` (communication-artifact flow: decks/brochures/one-pagers/reports), all format/language-agnostic and deferring the subjective *how* — all real, not symlinked — plus a Quant skill set (`quantitative-finance`, `financial-timeseries-analysis`, `backtesting-validation`, `quant-code-review`) and a **presentation pipeline**: `branding` (upstream durable identity, feeds all channels) → `presentation-design` (visual + copy *how*, inherits the brand) → a format-specific build tier (`deck-builder`, `one-pager-builder`, `brochure-builder`, `pamphlet-builder`, `report-builder`).
- Meta-skills built: `agent-authoring` (the single source of truth for layer-choice + the authoring checklist) + three specialized siblings `developer-`/`product-manager-`/`knowledge-agent-authoring`, now trimmed to a clean **delta** that points back to the base instead of restating it.
- Operational skills built: `session-memory`, `agent-finder`, `knowledge-router` (reference-notes tier via `scripts/context.py`), `token-optimizer` (work-placement; `scripts/tokens.py`), and `skill-distiller` (decide whether know-how → a skill: significance + redundancy gate via `scripts/skills.py`, prefers extending; hands authoring to `/add-skill`; plan-nudge hook on ExitPlanMode). Operational **agent**: `token-manager` (verbose work → capped summary). Context-economy **guard hooks**: `post_bash_filter.py` (PostToolUse·Bash) + `pre_read_guard.py` (PreToolUse·Read). **Single-sourced:** canonical copies in `example-project/.claude/`; the factory `.claude/` holds symlinks (skills, the agent, hook scripts + fragments, `build-hooks.py`) — edit in `example-project/`, never copy.
- Authoring commands built: the `add-*` family (`add-skill/agent/command/hook/workflow`).
- Version-labeling flow built (operational, single-sourced): `.meta/version` (root file, semver label + goals) + `ship-version` workflow + `/version-set` + `/version-ship` + `version_guard.py` (PreToolUse·Bash, opt-in-by-presence, warn-only). PR is named/shipped from the version's goals.
- **Memory:** `session-memory` skill + `.claude/memory/` now replace the old `DECISIONS.md` workflow; four lifecycle hooks (SessionStart/PreCompact/Stop/UserPromptSubmit).
- **Hooks** are authored as per-hook `*.json` fragments in `.claude/hooks/` and compiled into `settings.json` by `build-hooks.py`; self-maintaining via a PostToolUse auto-rebuild + a SessionStart staleness warning.
- **Capability catalog:** `.claude/hooks/catalog.py` (bare generator, like build-hooks.py) generates a per-tree on-demand `.claude/CATALOG.md` inventory of skills/agents/commands/workflows; kept fresh by PostToolUse `--on-edit` + SessionStart `--warn-if-stale` (warn-only) + the `/reindex` command. `CLAUDE.md` now references the catalog at category level instead of enumerating assets (skills/agents already auto-load by `description:`).
- **Factory efficiency:** Stop hook nudges at most once per session (per-`session_id` temp marker); `author-asset` workflow + `add-*` guardrails forbid re-deriving formats via Explore agents.
- **Distribution layer built** (`distribution/`): a remote Claude instance can now pull assets from this brain. `registry.json` (curated dep/companion overlay) → `build_library.py` generates root `LIBRARY.md` + `library.json` → `pull.py` copies an asset + transitive `requires` + companion hook fragments (and scripts they reference + `build-hooks.py`) from the canonical `example-project/.claude/` tree, **dereferencing symlinks** (sidesteps the copy-trap), writes `.brain-provenance.json`. Resolves the three frictions (buried home, symlink trap, no dep manifest). Factory capability, not a pullable asset.

## Decisions        (append-only; supersede, never delete)
- [2026-06-28] Built the **`distribution/` layer** (the remote-pull fix from the review below). New top-level dir (factory capability about the brain exporting itself; NOT a pullable asset): `registry.json` (hand-authored overlay — category/portable/requires/hooks, the deps that can't be auto-derived), `build_library.py` (catalog.py-style generator → root `LIBRARY.md` human index + `distribution/library.json` machine manifest; `--check`/`--warn-if-stale`; flags assets missing a registry entry as category `other`), `pull.py` (`pull.py <asset-id> --dest <target>/.claude [--dry-run]`: transitively expands `requires`, copies from canonical `example-project/.claude/` **dereferencing symlinks**, brings companion hook fragments + scripts they reference + `build-hooks.py`, writes `.brain-provenance.json` with brain_version). Key lever: canonical tree has REAL files (only repo-root `.claude/` is symlinked), so always pull from example-project. Verified end-to-end: knowledge-router pull → +session-memory+skill-distiller+4 hooks+scripts; deck-builder → +presentation-design+branding; token-optimizer → +guards; pulled fragments compiled by copied build-hooks.py into a valid settings.json (8 fragments). Signposted root README + CLAUDE.md (canonical-library statement + structure diagram) + `distribution/README.md`. 55 assets catalogued. General gaps (validator, tests/CI, hook-authoring meta-skill) deferred — out of scope — sessions/2026-06-28-0145-remote-instance-pull-review.md
- [2026-06-28] Review-only assessment of remote-instance asset pulling (a Claude in another project pulling skills/agents from this brain). Verdict: **partially compatible** — discoverable (description auto-load + CATALOG.md) and assets mostly liftable, but autonomous pulling has friction: (1) canonical products buried under `example-project/.claude/` not an obvious library; (2) repo-root `.claude/` symlink copy-trap (needs `cp -rL`); (3) no per-asset dependency/companion-hook manifest → partial pulls silently degrade. Top suggested fix (not built): machine-readable distribution manifest. User chose review-only; nothing implemented — sessions/2026-06-28-0145-remote-instance-pull-review.md
- [2026-06-21] Slimmed `example-project/CLAUDE.md` to a category-level **Capabilities** section + added a **capability-catalog** system. `catalog.py` (bare generator in hooks/, NOT a skill — a SKILL.md would auto-inject a description every session, the cost we're cutting) scans skills/agents/commands/workflows → per-tree `.claude/CATALOG.md` (generated, not symlinked, like settings.json). On-demand (referenced by path, never auto-printed); kept fresh by PostToolUse `--on-edit` + SessionStart `--warn-if-stale` + `/reindex`. Self-contained frontmatter parser duplicated from agents.py on purpose (no cross-bundle import); symlink-safe via `iterdir`/`is_file` + `os.path.samefile`. Symlinked into factory; both settings.json rebuilt. User chose on-demand + bare-script + full-collapse + `/reindex` via AskUserQuestion (switched from skill→bare-script when told skills load every session) — sessions/2026-06-21-1313-capability-catalog.md
- [2026-06-21] Built `report-builder` skill — fifth member of the format-specific build tier: long-form multi-page documents (reports/whitepapers/annual reports/proposals/manuals). Owns long-doc machinery: generated ToC/numbering/cross-refs/index, headers/footers/pagination/section-breaks/widow-orphan, footnotes + citations/bibliography; tools = Word/python-docx, LaTeX, Typst, Quarto/Pandoc, HTML→PDF, Google Docs. **Agent vs skill → skill** (symmetry; renders upstream content so not inherently context-flooding; per-job verbosity → delegate to `token-manager`, an orthogonal placement decision, not a reason to make it an agent). User first named it `long-form-report-building`, then asked to rename → `report-builder` for `*-builder` consistency. Branch off main (branch name still `claude/long-form-report-building`); PR #18 — sessions/2026-06-21-1224-report-builder.md
- [2026-06-21] Built `branding` skill — the durable, reusable identity layer **upstream** of the whole presentation pipeline (feeds all channels: decks/print/web/social/email). **One** skill (not fragmented into logo/color/voice — pillars are tightly coupled), and **standalone** (not folded into presentation-design — a brand outlives any one artifact). Four pillars: strategy / verbal / visual / governance. Boundary wired: `presentation-design`'s design-system step now *inherits* the brand (ad-hoc fallback when none); voice/tone set here, applied by presentation-design's copy craft. User chose name `branding`. PR #17 (branch off main; PR #16 already merged) — sessions/2026-06-21-1206-branding-skill.md
- [2026-06-20] Built the **format-specific build tier** below `presentation-design`: `one-pager-builder` (flat single page; page geometry/bleed/print-vs-screen), `brochure-builder` (folded single sheet; fold imposition — panels print in fold-position order, tuck-in panel narrower), `pamphlet-builder` (multi-page booklet/leaflet; booklet imposition — pages ×4, author in reader order + let tool impose, gutter/creep). User chose **three separate skills** (over one consolidated `print-document-builder` or a two-way folded/flat split) via AskUserQuestion — accepted the trigger/tool overlap for max specificity. Differentiated each by its genuine format-specific build concern; generalized `presentation-design` downstream refs + agent hand-off to the whole builder family. All in PR #16 — sessions/2026-06-20-1905-presentation-architect-agent.md
- [2026-06-20] Built the two executor skills below `presentation-architect`, completing a three-tier presentation pipeline: `presentation-design` (subjective visual + copy *how* — hierarchy/layout/type/color/data-viz/composition + final copy; format-spanning, tool-agnostic; emits a design spec) → `deck-builder` (execution tier — assemble a real deck file in a tool: pptx/python-pptx, Google Slides, Beamer, Marp/reveal.js; reproducible + verified). Clean no-overlap split: architect=flow, design=look/words, builder=file. Both consumer skills (real, not symlinked); registered in example-project CLAUDE.md; agent hand-off points at them. Shipped in PR #16 (title/body widened to cover agent + both skills) — sessions/2026-06-20-1905-presentation-architect-agent.md
- [2026-06-20] Built `presentation-architect` agent — third objective/conceptual agent (sibling of data-analyst + software-architect). Owns the **objective** flow of communication artifacts (decks/brochures/one-pagers/reports): audience+goal, narrative arc, message hierarchy, ordered per-unit content spec, reading path, CTA placement. Format- and tool-agnostic; defers the subjective *how* (visual design, final copy, authoring tool) to a design/content executor or skill (read-only `plan`, opus). User chose name `presentation-architect` + defer-the-how scope via AskUserQuestion. Consumer showcase agent (real, not symlinked). No companion executor skill exists yet — natural follow-up — sessions/2026-06-20-1905-presentation-architect-agent.md
- [2026-06-19] Built `software-architect` agent — second objective/conceptual agent (sibling of data-analyst). Owns the **objective** design of project STRUCTURE in service of the objective: architecture/boundaries, placement (where things live), naming conventions, file/module organization. Objective/subjective boundary: designs structure+conventions (read-only `plan`, opus) and defers the subjective code/framework AND the product scope (objective is an input). Consumer showcase agent (real, not symlinked); could be symlinked to let the factory dogfood it — sessions/2026-06-19-2123-software-architect-agent.md
- [2026-06-19] Built `data-analyst` agent — a conceptual, language/tool-agnostic agent owning the **objective** analysis method (frame→explore→visualize→conclude→hand off) with an explicit objective/subjective boundary: it specifies the analysis + concludes (read-only `plan`, opus) and defers the subjective *how* (language/library/code) to a developer agent or language skill. Consumer showcase agent (real in example-project, NOT symlinked — factory doesn't analyze data); objective counterpart to `finance-quantitative-developer` — sessions/2026-06-19-2108-data-analyst-agent.md
- [2026-06-19] Added version-labeling flow (`.meta/version` + `ship-version` workflow + `/version-set`/`/version-ship` + `version_guard.py`). Semver labels; single-sourced via symlink (first single-sourced workflow+commands). PR named/shipped from the version's goals. Guard is opt-in-by-presence + warn-only (silent when `.meta/version` absent, so the factory is unaffected). `.meta/version` = per-project state like memory/; sample committed in example-project — sessions/2026-06-19-0011-version-labeling.md
- [2026-06-18] Added `skill-distiller` operational skill (from uploads): the "should this become a skill?" gate (significance + lexical redundancy via `scripts/skills.py`, prefers extending) + a PostToolUse·ExitPlanMode plan-nudge. Single-sourced via symlink; exec-form hook. Closed the loop — repointed knowledge-router's procedure bullet back to skill-distiller (PR #10 had diverted it since the skill didn't exist). Adapted `skill-creator`→`/add-skill`/`author-asset`; fixed `skills.py` to `os.walk(followlinks=True)` so the factory's symlinked skills are visible to dedupe — sessions/2026-06-18-2254-skill-distiller.md
- [2026-06-18] Added token/context-economy tooling (from uploads): `token-optimizer` skill (+`tokens.py`), `token-manager` agent (first operational agent), and two fail-safe guard hooks — `post_bash_filter.py` (elide long Bash output) + `pre_read_guard.py` (cap huge unpaged reads, first PreToolUse hook). Single-sourced via symlink; both settings.json rebuilt (9 fragments). Bundle was internally consistent → no taxonomy rewrites (only `python3`→`python` in SKILL examples) — sessions/2026-06-18-2239-token-economy-tooling.md
- [2026-06-18] Added `knowledge-router` operational skill + `.claude/context/` reference-notes tier (model contextualization, from user uploads): `context.py` manages `INDEX.md` + `notes/`; SessionStart surfaces the index via exec-form hook + new `context.py index` (mirrors memory.py). Single-sourced via symlink per PR #9. Adapted the SKILL's references (`skill-distiller`→`author-asset`/`add-skill`; `.claude/rules/`→`CLAUDE.md`/context note) to claudeBrain's real taxonomy — sessions/2026-06-18-2227-knowledge-router-context-tier.md
- [2026-06-18] Single-sourced the operational assets: canonical copies in `example-project/.claude/`, factory holds 9 relative symlinks (`skills/session-memory`, `skills/agent-finder`, `hooks/build-hooks.py` + 6 shared fragments). Kills factory↔consumer drift (caught `example-project` `memory.py` stale — missing the PR #7 Stop-hook dedupe). Liftability preserved (example-project files are real; `cp -rL` works). **Supersedes** the [2026-06-18] "left example-project script dup as intentional" stance for these specific assets. `settings.json` + per-layer READMEs stay independent — sessions/2026-06-18-2200-single-source-operational-assets.md
- [2026-06-18] Second efficiency pass: de-duped the agent-authoring family to base+delta (siblings now point to the base's *Is an agent the right tool?* + checklist instead of restating; "Start with agent-authoring" intro compressed) and trimmed all 4 eager descriptions (kept every trigger list). Deliberately left example-project script dup (intentional showcase), hook micro-perf, and command placement boilerplate alone. −37 lines, settings.json unchanged — sessions/2026-06-18-2140-optimize-agent-authoring-family.md
- [2026-06-18] Factory-efficiency pass: Stop hook deduped to once/session (temp marker keyed by session_id, supersedes block-every-Stop); new `author-asset` workflow + `add-agent`/`add-skill` guardrails ban re-deriving formats via Explore agents — sessions/2026-06-18-2059-optimize-factory-model.md
- [2026-06-17] First consumer agent + quant skill layer: `finance-quantitative-developer` (Python/numpy/scipy/pandas, verification-gated) + 4 skills in example-project; stack=Python (not C#/VSTO), built on `developer-agent-authoring` — sessions/2026-06-17-1753-finance-quant-dev-agent.md
- [2026-06-17] Permissions: `defaultMode: acceptEdits` (no edit prompts) + leading-slash `Edit(/.claude/**)`; Bash **scoped** to session-used families (supersedes the broad-Bash grant) — sessions/2026-06-17-permission-tuning.md
- [2026-06-17] Added `agent-finder` operational skill (subagent selection) + `agents.py`; granted broad `Bash` permission in committed settings.json per user request — sessions/2026-06-17-agent-finder.md
- [2026-06-17] Hook drift guard — PostToolUse auto-rebuilds settings.json on fragment edit + SessionStart warns if stale; `is_fragment` uses `os.path.samefile` for path-format robustness — sessions/2026-06-17-hook-guard-and-csharp.md
- [2026-06-17] example-project `coding-standards` skill uses Python + C# (its real VSTO/.NET stack); dropped TypeScript — sessions/2026-06-17-hook-guard-and-csharp.md
- [2026-06-17] Hooks stored as per-hook `*.json` fragments compiled into settings.json by `build-hooks.py` — Claude Code has no native external-hook loading; plugins (the only alternative) add install friction — sessions/2026-06-17-hook-fragments.md
- [2026-06-17] Adopt `session-memory`, retire `DECISIONS.md` — bounded decay + append-only logs beat a rewrite-in-place log — sessions/2026-06-17-adopt-session-memory.md
- [2026-06-17] Factory authoring commands: the `add-*` family — one-shot scaffolders per layer, delegate depth to meta-skills/READMEs — sessions/2026-archive-decisions.md
- [2026-06-17] Fourth meta-skill `knowledge-agent-authoring` — curator/retriever agents; spans read-only + curation modes — sessions/2026-archive-decisions.md
- [2026-06-16] Third meta-skill `product-manager-agent-authoring` — read-only project-altitude assessor sibling — sessions/2026-archive-decisions.md
- [2026-06-16] Second meta-skill `developer-agent-authoring` — language/stack code-writer with verification gate — sessions/2026-archive-decisions.md
- [2026-06-12] First meta-skill `agent-authoring` — grounded in durable agent fields, defers volatile long tail to docs — sessions/2026-archive-decisions.md
- [2026-06-11] Split into factory (`.claude/`) + mock consumer (`example-project/`) — design env vs. its output — sessions/2026-archive-decisions.md
- [2026-06-11] Adopt the typed `.claude/` layout; empty layers scaffolded, not fabricated — sessions/2026-archive-decisions.md

## Threads          (open items; remove when closed)
- PR #7 (first factory-efficiency pass) merged. `claude/optimize-factory-model` re-cut from main for the second efficiency pass (agent-authoring dedup); pushed, no new PR opened yet — open one if wanted.
- Planned meta-skills not yet written: `skill-authoring`, `context-vs-skill`.
- Planned commands: `/validate-asset`, `/add-context`.
- Possible future agent siblings: a line-level code-reviewer; an orchestrator/coordinator.
- `/add-skill` references `skill-authoring` (planned); it falls back to README + `agent-authoring` until that lands.

## Log              (append-only pointers)
- 2026-06-28 | Built distribution/ layer (registry + build_library.py + pull.py + LIBRARY.md; remote-pull enabled) | sessions/2026-06-28-0145-remote-instance-pull-review.md
- 2026-06-28 | Review: remote-instance asset-pull compatibility (review only; distribution gap + manifest suggestion) | sessions/2026-06-28-0145-remote-instance-pull-review.md
- 2026-06-21 | README architecture update (root + consumer .claude/README; PR #20) | sessions/2026-06-21-1320-readme-architecture-update.md
- 2026-06-21 | capability-catalog: slim CLAUDE.md + catalog.py/CATALOG.md/reindex + 2 hooks | sessions/2026-06-21-1313-capability-catalog.md
- 2026-06-21 | report-builder skill (long-form document build tier; PR #18) | sessions/2026-06-21-1224-report-builder.md
- 2026-06-21 | branding skill (upstream identity layer; presentation-design inherits it; PR #17) | sessions/2026-06-21-1206-branding-skill.md
- 2026-06-20 | one-pager/brochure/pamphlet-builder skills (format-specific build tier; PR #16) | sessions/2026-06-20-1905-presentation-architect-agent.md
- 2026-06-20 | presentation-design + deck-builder skills (executor tiers below the architect; PR #16) | sessions/2026-06-20-1905-presentation-architect-agent.md
- 2026-06-20 | presentation-architect agent (objective communication-artifact flow; defers subjective how) | sessions/2026-06-20-1905-presentation-architect-agent.md
- 2026-06-19 | software-architect agent (objective project structure/placement/naming/conventions) | sessions/2026-06-19-2123-software-architect-agent.md
- 2026-06-19 | data-analyst agent (objective explore/visualize/conclude; defers subjective how) | sessions/2026-06-19-2108-data-analyst-agent.md
- 2026-06-19 | version-labeling flow: .meta/version + ship-version workflow + commands + guard hook | sessions/2026-06-19-0011-version-labeling.md
- 2026-06-18 | skill-distiller skill (when to create/extend a skill) + plan-nudge hook | sessions/2026-06-18-2254-skill-distiller.md
- 2026-06-18 | token-economy tooling: token-optimizer skill + token-manager agent + guard hooks | sessions/2026-06-18-2239-token-economy-tooling.md
- 2026-06-18 | knowledge-router skill + .claude/context/ reference-notes tier | sessions/2026-06-18-2227-knowledge-router-context-tier.md
- 2026-06-18 | Single-source operational assets via symlink (kill factory↔consumer drift) | sessions/2026-06-18-2200-single-source-operational-assets.md
- 2026-06-18 | Dedupe agent-authoring family to base+delta + trim eager descriptions | sessions/2026-06-18-2140-optimize-agent-authoring-family.md
- 2026-06-18 | Factory efficiency: Stop-hook dedupe + author-asset workflow | sessions/2026-06-18-2059-optimize-factory-model.md
- 2026-06-17 | finance-quantitative-developer agent + 4 quant skills (consumer) | sessions/2026-06-17-1753-finance-quant-dev-agent.md
- 2026-06-17 | Permission tuning: acceptEdits + scoped Bash | sessions/2026-06-17-permission-tuning.md
- 2026-06-17 | Add agent-finder skill + grant Bash permission | sessions/2026-06-17-agent-finder.md
- 2026-06-17 | Hook drift guard + coding-standards → Python/C# | sessions/2026-06-17-hook-guard-and-csharp.md
- 2026-06-17 | Hooks as fragments + build-hooks.py generator | sessions/2026-06-17-hook-fragments.md
- 2026-06-17 | Adopt session-memory; migrate DECISIONS.md | sessions/2026-06-17-adopt-session-memory.md
- (pre-2026-06-17) | Full prior decision history (8 entries) | sessions/2026-archive-decisions.md
