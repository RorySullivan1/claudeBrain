# Stage 01 — Foundation

**Goal:** stand up the repo so the add-in builds and deploys and the Python tooling runs —
the ground every later stage stands on.

**Versions:** v0.1.0 (skeleton + ClickOnce build path), v0.2.0 (Python tooling base).

**Milestone (stage exit):** a clean machine can build the add-in, deploy it via ClickOnce,
and run the Python tooling end-to-end. Met — stage shipped.
