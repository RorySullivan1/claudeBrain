# version: v0.1.0
status: shipped

## Goals
- Lay out the repo skeleton (`src/AddIn/`, `tools/`, `docs/`) and the VSTO add-in shell.
- Establish the ClickOnce build/publish path under the no-admin constraint.

## Objectives / acceptance
- The add-in solution builds and produces a ClickOnce package.
- A clean Windows profile can install and load the add-in shell.
