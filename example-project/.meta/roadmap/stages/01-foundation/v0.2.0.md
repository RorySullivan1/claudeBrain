# version: v0.2.0
status: shipped

## Goals
- Build the Python tooling base in `tools/` — data-prep and build/packaging scripts.
- Pin the interpreter and wire the formatter/linter/test commands.

## Objectives / acceptance
- `tools/` runs against the pinned interpreter; `ruff` clean; `pytest` green.
- The packaging script stages the ClickOnce artifacts reproducibly.
