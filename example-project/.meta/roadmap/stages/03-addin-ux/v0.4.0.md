# version: v0.4.0
status: planned

## Goals
- Add a ribbon tab with a "Run analytics" button and a task pane to show results.
- Call the v0.3.0 analytics surface from the add-in and render the output in the pane.

## Objectives / acceptance
- The ribbon button is present and invokes the analytics on the active workbook's inputs.
- Results render in the task pane; a smoke test confirms the round-trip on a sample workbook.
- COM objects released correctly (no leaked references; Excel closes cleanly after use).
- No regression in the ClickOnce build/deploy path.
