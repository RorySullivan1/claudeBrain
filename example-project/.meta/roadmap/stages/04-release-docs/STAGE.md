# Stage 04 — Release & docs  (sketch)

**Goal:** get a signed, documented v1.0 into the desk's hands. Detail this stage with
`/roadmap-set` when stage 03 is near done — kept as intent for now.

**Versions (sketch):** v1.0.0 — signed ClickOnce release + user guide and developer docs.

**Milestone (stage exit):** the desk installs v1.0 from the trusted location and follows the
user guide unaided; contributors have developer docs to build and ship updates.
